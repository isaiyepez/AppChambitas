@model IEnumerable<AppChambitasV1.Domain.ServiciosTecnicos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ServTecn_Activo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ServTecn_Activo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServTecn_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServTecn_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServTecn_ID })
        </td>
    </tr>
}

</table>
